---
description: Launch the server-architect agent to implement backend server changes (domain, use case, infrastructure, handler layers)
---

Use the Task tool with subagent_type=server-architect to implement the following backend task.

If the user provided additional arguments after the command, use those as the task description. Otherwise, ask the user what backend feature they want to implement.

